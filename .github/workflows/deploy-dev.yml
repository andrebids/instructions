name: Deploy (dev) via SSH

on:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Write SSH key
        run: |
          mkdir -p ~/.ssh
          echo "$SSH_KEY" > ~/.ssh/id_deploy
          chmod 600 ~/.ssh/id_deploy
          echo "🔑 Verificando chave SSH escrita..."
          echo "Tamanho do ficheiro: $(wc -c < ~/.ssh/id_deploy) bytes"
          echo "Primeiras linhas:"
          head -n 2 ~/.ssh/id_deploy
          echo "Últimas linhas:"
          tail -n 2 ~/.ssh/id_deploy
          echo "Tentando extrair chave pública..."
          ssh-keygen -y -f ~/.ssh/id_deploy 2>&1 | head -n 1 || echo "⚠️ Não foi possível extrair chave pública (formato pode estar incorreto)"
        shell: bash
        env:
          SSH_KEY: ${{ secrets.SSH_KEY }}

      - name: Add remote host to known_hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -p "${{ secrets.SSH_PORT }}" "${{ secrets.SSH_HOST }}" >> ~/.ssh/known_hosts
        shell: bash

      - name: Garantir rsync no servidor
        run: |
          echo "📦 Verificando se rsync está instalado no servidor..."
          ssh -i ~/.ssh/id_deploy -p "${{ secrets.SSH_PORT }}" -o StrictHostKeyChecking=no "${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}" "command -v rsync >/dev/null 2>&1 || (sudo apt-get update -y && sudo apt-get install -y rsync)" || echo "⚠️ Não foi possível instalar rsync"
        shell: bash

      - name: Rsync código para o servidor
        run: |
          echo "🔑 Verificando chave SSH..."
          ls -la ~/.ssh/id_deploy || echo "⚠️ Chave SSH não encontrada"
          echo "🔍 Testando conexão SSH..."
          ssh -i ~/.ssh/id_deploy -p "${{ secrets.SSH_PORT }}" -o StrictHostKeyChecking=no -o ConnectTimeout=10 "${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}" "echo 'SSH connection OK'" || echo "⚠️ SSH test falhou"
          
          RSYNC_EXCLUDES=(
            ".git/"
            "**/node_modules/"
            "**/.next/"
            "**/dist/"
            "**/build/"
          )
          EXCLUDE_ARGS=""
          for e in "${RSYNC_EXCLUDES[@]}"; do EXCLUDE_ARGS+=" --exclude=$e"; done
          
          echo "📤 Iniciando rsync..."
          rsync -avz --delete $EXCLUDE_ARGS \
            -e "ssh -i ~/.ssh/id_deploy -p ${{ secrets.SSH_PORT }} -o StrictHostKeyChecking=no -o LogLevel=VERBOSE" \
            "${GITHUB_WORKSPACE}/" "${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:${{ secrets.REMOTE_PATH }}/" || {
            echo "❌ Rsync falhou!"
            echo "Tentando debug SSH..."
            ssh -i ~/.ssh/id_deploy -p "${{ secrets.SSH_PORT }}" -v "${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}" "echo test" || true
            exit 1
          }
        shell: bash

      - name: Deploy completo no servidor
        run: |
          ssh -i ~/.ssh/id_deploy -p "${{ secrets.SSH_PORT }}" "${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}" bash << REMOTE_SCRIPT
          set -e
          set -x
          REMOTE_PATH="${{ secrets.REMOTE_PATH }}"
          VITE_CLERK_PUBLISHABLE_KEY="${{ secrets.VITE_CLERK_PUBLISHABLE_KEY }}"
          # Expandir ~ para caminho absoluto se necessário
          if [[ "\$REMOTE_PATH" == ~* ]]; then
            REMOTE_PATH="\${REMOTE_PATH/#\~/\$HOME}"
          fi
          # Guardar o caminho absoluto
          REMOTE_PATH="\$(cd "\$REMOTE_PATH" && pwd)"
          cd "\$REMOTE_PATH"
          echo "📍 Diretório atual: \$REMOTE_PATH"
          echo "📍 PWD: \$(pwd)"
          
          # 1. Garantir Docker, Docker Compose e rsync
          echo "🐳 Verificando Docker..."
          if ! command -v docker >/dev/null 2>&1; then
            echo "Instalando Docker..."
            curl -fsSL https://get.docker.com | sh
          fi
          if ! docker compose version >/dev/null 2>&1; then
            if command -v apt-get >/dev/null 2>&1; then 
              echo "Instalando Docker Compose..."
              sudo apt-get update -y 
              sudo apt-get install -y docker-compose-plugin
            fi
          fi
          echo "📦 Verificando rsync..."
          if ! command -v rsync >/dev/null 2>&1; then
            echo "Instalando rsync..."
            sudo apt-get update -y
            sudo apt-get install -y rsync || echo "⚠️ Falha ao instalar rsync"
          fi
          
          # 2. Subir Postgres via Docker Compose
          echo "🐘 Subindo Postgres..."
          docker compose -f instructions-project/docker-compose.dev.yml up -d --pull always || echo "⚠️ Docker Compose pode ter falhado, mas continuando..."
          
          # 3. Configurar client/.env ANTES do build (obrigatório para Vite)
          echo "🔐 Configurando variáveis de ambiente do cliente..."
          cd instructions-project/client
          # Criar ou atualizar o arquivo .env com a chave Clerk
          if [ -n "\$VITE_CLERK_PUBLISHABLE_KEY" ]; then
            # Se o arquivo já existe, preservar outras variáveis e atualizar apenas VITE_CLERK_PUBLISHABLE_KEY
            if [ -f .env ]; then
              # Remover linha antiga da chave Clerk se existir
              grep -v "^VITE_CLERK_PUBLISHABLE_KEY=" .env > .env.tmp || true
              mv .env.tmp .env
            else
              # Criar arquivo novo se não existir
              touch .env
            fi
            # Adicionar a chave Clerk ao final do arquivo
            echo "VITE_CLERK_PUBLISHABLE_KEY=\$VITE_CLERK_PUBLISHABLE_KEY" >> .env
            echo "✅ VITE_CLERK_PUBLISHABLE_KEY configurada no client/.env"
          else
            echo "⚠️ VITE_CLERK_PUBLISHABLE_KEY não foi fornecida como secret"
            exit 1
          fi
          
          # 4. Instalar dependências do client e fazer build
          echo "📦 Fazendo build do frontend..."
          npm ci || npm install || echo "⚠️ npm install falhou, mas continuando..."
          npm run build || { echo "❌ Build do frontend falhou!"; exit 1; }
          
          # 5. Instalar dependências do server
          echo "📦 Instalando dependências do servidor..."
          cd ../server
          npm ci --omit=dev || npm install --omit=dev || echo "⚠️ npm install falhou, mas continuando..."
          
          # 5.5. Executar migration para adicionar campos de canvas (se necessário)
          echo "🔄 Executando migration de campos de canvas..."
          npm run migrate || echo "⚠️ Migration falhou ou campos já existem, continuando..."
          
          # 6. Garantir que server/.env existe (preservar se já existir - não sobrescrever se existir)
          if [ ! -f .env ]; then
            printf "DB_HOST=localhost\nDB_PORT=5433\nDB_NAME=instructions_demo\nDB_USER=demo_user\nDB_PASSWORD=demo_password\nPORT=5000\nNODE_ENV=production\n" > .env
            echo "⚠️ Ficheiro server/.env criado. Por favor configure CLERK_SECRET_KEY manualmente se necessário."
          fi
          
          # 7. Reiniciar servidor Node.js via PM2
          echo "🔄 Reiniciando servidor Node.js..."
          cd instructions-project/server
          if command -v pm2 >/dev/null 2>&1; then
            pm2 restart instructions-server --update-env 2>/dev/null || pm2 start "node src/app.js" --name instructions-server --cwd . || true
            pm2 save 2>/dev/null || true
          else
            echo "⚠️ PM2 não encontrado. Instalando..."
            sudo npm i -g pm2 || true
            pm2 start "node src/app.js" --name instructions-server --cwd . || true
            pm2 save 2>/dev/null || true
          fi
          
          # 8. Recarregar Nginx para servir novos ficheiros
          echo "🔄 Recarregando Nginx..."
          sudo systemctl reload nginx || true
          
          # 9. Verificar se serviços estão a correr
          echo "🔍 Verificando status dos serviços..."
          docker ps --filter "name=postgres" --format "table {{.Names}}\t{{.Status}}" || true
          pm2 list | grep instructions-server || echo "⚠️ PM2 processo não encontrado"
          
          echo "✅ Deploy concluído com sucesso!"
          echo "📍 Timestamp: $(date)"
          REMOTE_SCRIPT
        shell: bash
      
      - name: Verificar health do servidor
        run: |
          sleep 5
          echo "🔍 Verificando health check..."
          curl -f https://thecore.dsproject.pt/health || echo "⚠️ Health check falhou - servidor pode ainda estar a iniciar"
        continue-on-error: true


