# ============================================
# Database Configuration (Supabase)
# ============================================
# Supabase connection pooler URL for database
DATABASE_URL=postgresql://postgres.[PROJECT_REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres

# Supabase project URL
SUPABASE_URL=https://[PROJECT_REF].supabase.co

# Supabase service role key (keep secret!)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ============================================
# Server Configuration
# ============================================
# Port for the backend server (internal to container)
PORT=5000

# Node environment (development or production)
NODE_ENV=development

# DNS resolution order (for IPv4 preference)
NODE_OPTIONS=--dns-result-order=ipv4first

# ============================================
# Authentication (Clerk / Auth.js)
# ============================================
# Clerk publishable key (if using Clerk)
VITE_CLERK_PUBLISHABLE_KEY=pk_test_...

# Use Auth.js instead of Clerk
USE_AUTH_JS=true

# Auth.js secret (generate with: openssl rand -base64 32)
AUTH_SECRET=your-secret-key-here

# Auth URL (adjust for Docker if needed)
# For development: http://localhost:5001
# For production: https://your-domain.com
AUTH_URL=http://localhost:5000

# ============================================
# Email Configuration (SMTP)
# ============================================
# Email service provider
EMAIL_SERVICE=smtp

# SMTP host
EMAIL_HOST=mail.gandi.net

# SMTP port (usually 587 for TLS, 465 for SSL)
EMAIL_PORT=587

# SMTP username
EMAIL_USER=your-email@example.com

# SMTP password
EMAIL_PASSWORD=your-email-password

# From address for emails
EMAIL_FROM=noreply@example.com

# ============================================
# Frontend Configuration
# ============================================
# Frontend URL (adjust for Docker if needed)
# For development: http://localhost:3003
# For production: https://your-domain.com
FRONTEND_URL=http://localhost:3003

# ============================================
# Optional: Additional Configuration
# ============================================
# JWT secret for custom authentication
# JWT_SECRET=your-jwt-secret-here

# Session secret
# SESSION_SECRET=your-session-secret-here

# API rate limiting
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100

# File upload limits (in bytes)
# MAX_FILE_SIZE=10485760

# Logging level (error, warn, info, debug)
# LOG_LEVEL=info
